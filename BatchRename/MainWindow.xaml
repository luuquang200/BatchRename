<Window x:Class="BatchRename.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BatchRename"
        mc:Ignorable="d"
        x:Name="wdMain"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <Window.Resources>
        <local:PreviewRenameConverter x:Key="PreviewRenameConverter"/>
    </Window.Resources>

    <!--<Canvas>
        <Button x:Name="loadPresetButton" Width="80" Height="35" Content="Load preset" Click="loadPresetButton_Click" Canvas.Left="555" Canvas.Top="41"/>
        <Button x:Name="addFileButton" Width="80" Height="35" Content="Browse.." Canvas.Left="233" Canvas.Top="41" Click="addFileButton_Click"/>
        <ListView x:Name="sourceListView" Width="200" Height="300" Canvas.Left="233" Canvas.Top="93">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ShortName}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ListView x:Name="previewListView" Width="200" Height="300" HorizontalAlignment="Left" Canvas.Left="555" Canvas.Top="93" VerticalAlignment="Top">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ShortName, Converter={StaticResource converter}}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Canvas>-->

    <Grid>
        <StackPanel Orientation="Horizontal">
            
            <StackPanel Orientation="Vertical">
                <ListView x:Name="listViewRulesApply" Width="200" Height="300"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          ScrollViewer.VerticalScrollBarVisibility="Visible"
                          HorizontalAlignment="Center" d:ItemsSource="{d:SampleData ItemCount=5}"
                          PreviewMouseLeftButtonDown="List_PreviewLeftMouseDown">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="border"
                                BorderBrush="Gray"
                                BorderThickness="2"
                                CornerRadius="3">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                        <GradientStop Color="Transparent" Offset="1.0" />
                                    </LinearGradientBrush>
                                </Border.Background>
                                <StackPanel Orientation="Horizontal"
                                        Width="180"
                                        Height="30">
                                    <TextBlock Text="{Binding NameRule}"/>
                                    <Button x:Name="buttonConfig"
                                            Width="50"
                                            Height="25"
                                            Margin="5 0 0 0"
                                            HorizontalAlignment="Right"
                                            Content="config"
                                            Click="buttonConfig_Click"/>
                                </StackPanel>
                            </Border>

                            <DataTemplate.Triggers>
                                <Trigger Property="IsMouseOver"
                                 Value="True">
                                    <Setter Property="BorderBrush"
                                    TargetName="border"
                                    Value="Black" />

                                    <Setter Property="BorderThickness"
                                    TargetName="border"
                                    Value="2" />
                                </Trigger>
                                <DataTrigger Binding="{Binding Path=IsSelected, RelativeSource={
                                          RelativeSource Mode=FindAncestor, AncestorType={x:Type ListViewItem }}}"
                                          Value="True">
                                    <Setter Property="BorderBrush"
                                    TargetName="border"
                                    Value="#0033A1" />
                                    <Setter Property="Cursor" Value="Hand" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListView.ItemTemplate>

                    <ListView.ItemContainerStyle>
                        <Style TargetType="{x:Type ListViewItem}">
                            <!--remove green selected area-->
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ListViewItem}">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Tag" Value="{Binding ElementName=wdMain}" />
                            <Setter Property="Margin" Value="5" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </StackPanel>

            <!--<ListView x:Name="sourceListView" Width="200" Height="300" VerticalAlignment="Top">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ShortName}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel Orientation="Vertical">
                <ListView x:Name="previewListView" Width="200" Height="300" VerticalAlignment="Top">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ShortName, Converter={StaticResource PreviewRenameConverter}}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>-->
            <GroupBox HorizontalAlignment="Left" Height="340" Header="File" Margin="10" VerticalAlignment="Top" Width="410">
                <ListView x:Name="listViewFile" HorizontalAlignment="Left" Height="305" VerticalAlignment="Top" Width="410">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Filename" Width="100" DisplayMemberBinding="{Binding ShortName}" />
                            <GridViewColumn Header="New Filename" Width="120" DisplayMemberBinding="{Binding ShortName, Converter={StaticResource PreviewRenameConverter}}" />
                            <GridViewColumn Header= "Path" Width="120" DisplayMemberBinding="{Binding a}"/>
                            <GridViewColumn Header= "Note" Width="60" DisplayMemberBinding="{Binding a}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </GroupBox>

            <StackPanel Orientation="Vertical">
                <Button x:Name="loadPresetButton" Width="80" Height="35" Content="Load preset" Margin="10"/>
                <Button x:Name="addFileButton" Width="80" Height="35" Content="Browse.." 
                    Margin="10"
                    Click="addFileButton_Click"/>
                <Button x:Name="applyButton" Margin="10"
                    Width="80" Height="35" Content="Apply" Click="applyButton_Click"/>
            </StackPanel>
           
        </StackPanel>
        
    </Grid>
</Window>
